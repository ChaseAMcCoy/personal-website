// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://chasemccoy.dev', // your custom domain
    base: '/', // root path
    outDir: './dist',
});